; orx-demo - Basic template config file

; Loads regular config data
@orx-demo.ini@

; Overrides some values for profile
[Display]
Title           = orx-demo (Profile)

[Render]
ShowFPS         = true

; orx-demo - Basic template config file

; Loads regular config data
@orx-demo.ini@

; Overrides some values for debug
[Display]
Title           = orx-demo (Debug)

[Render]
ShowFPS         = true

[Resource]
WatchList       = Config # Texture # Sound
